import React from 'react';
import ReactDOM from 'react-dom/client';
import TgBotDesigner from './TgBotDesignerPrototype';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TgBotDesigner />
  </React.StrictMode>
);